{
  "name": "buffet",
  "module": "server/index.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot server/index.ts",
    "build": "rm -rf dist && bun build --production --outdir=dist public/index.html",
    "start": "ENV=production bun run server/index.ts",
    "format": "biome format --write",
    "lint": "biome check . && svelte-check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.3",
    "@types/bun": "latest",
    "bun-plugin-svelte": "latest",
    "bun-plugin-tailwind": "latest",
    "svelte-check": "latest"
  },
  "dependencies": {
    "@effect/platform": "latest",
    "@effect/platform-bun": "latest",
    "@effect/rpc": "latest",
    "@effect/sql-sqlite-bun": "latest",
    "clsx": "latest",
    "effect": "3.19.2",
    "svelte": "latest",
    "tailwind-merge": "latest",
    "tailwind-variants": "latest",
    "tailwindcss": "latest",
    "tw-animate-css": "latest"
  }
}
